# Selected Poster Assets

Recommended main figures:

- `feature_pipeline.pdf` / `feature_pipeline.png`: explains how raw frame state becomes canonical tokens and a 561-dim policy input.
- `policy_architecture.pdf` / `policy_architecture.png`: explains the token encoder, LSTM, pointer heads, and value critic.
- `training_curriculum.pdf` / `training_curriculum.png`: explains the staged training curriculum from basic control to lane aggression and win conversion.

Recommended supporting evidence:

- `leaderboard_card.png`: concise external result, Rank #3 and 66.2% aggregate win rate.
- `lane_pressure.png`: behavior evidence for lane pressure and kill-death conversion.
- `historical_probe_matrix.png`: code-history probe evidence for feature semantics.

Not selected as a primary poster figure:

- `training_progress.png`: the reward panel connects final reward snapshots across different experiments/configurations, so it can imply a misleading downward trend.
